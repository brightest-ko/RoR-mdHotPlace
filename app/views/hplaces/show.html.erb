<script type="text/javascript">
$(function(){
  var cate_food = $("#cate_food");
  var cate_cafe = $("#cate_cafe");
  var cate_culture = $("#cate_culture");
  <%if @hplace.category == 'cafe'%>
  cate_cafe.addClass('active');
  cate_culture.removeClass('active');
  cate_food.removeClass('active');
  <%elsif @hplace.category == 'culture'%>
  cate_cafe.removeClass('active');
  cate_culture.addClass('active');
  cate_food.removeClass('active');
  <%else @hplace.category == 'food'%>
  cate_cafe.removeClass('active');
  cate_culture.removeClass('active');
  cate_food.addClass('active');
  <%end%>
});
</script>
<style>
.star4{font-size: 2.7em; color: #FCD201;}
.star5{font-size: 2.7em; color: #828180;}
.star3{font-size: 2em; color: silver;}
.star2{font-size: 2em; color: orange;}

</style>

<div class="container">
    <table>
    <tr><td><h1><%=@hplace.place_name%></h1></td>
    <td>   
    <%if user2_signed_in?%>
      <%if @albookmark.nil?%>
      <span class='glyphicon glyphicon-star-empty star5' onclick="location.href='/bookmarks/create/<%=current_user2.id%>/<%=@hplace.id%>?click=bookmark'" name="uid"></span>
      <%end%>
      <%if !@albookmark.nil?%>
      <span class='glyphicon glyphicon-star star4'onclick="location.href='/bookmarks/destroy/<%=current_user2.id%>/<%=@hplace.id%>?click=des'" name="uid" ></span>
      <%end%>
    <%end%></td>
    
    </tr></table>
 
    
    <div><i class="glyphicon glyphicon-eye-open"></i> : <%=@hplace.views%> | 리뷰 : <%=@hplace.reviewcount%> | 별로 나타내기<%=@hplace.avg_evalu%> </div>

      <%if @hplace.avg_evalu.to_i  < 1%>
      <span class='glyphicon glyphicon-star-empty star3'></span>
      <%else%>
      <span class='glyphicon glyphicon-star star2'></span>
      <%end%>
      <%if @hplace.avg_evalu.to_i  < 2%>
      <span class='glyphicon glyphicon-star-empty star3'></span>
      <%else%>
      <span class='glyphicon glyphicon-star star2'></span>
      <%end%>
      <%if @hplace.avg_evalu.to_i  < 3%>
      <span class='glyphicon glyphicon-star-empty star3'></span>
      <%else%>
      <span class='glyphicon glyphicon-star star2'></span>
      <%end%>
      <%if @hplace.avg_evalu.to_i  < 4%>
      <span class='glyphicon glyphicon-star-empty star3'></span>
      <%else%>
      <span class='glyphicon glyphicon-star star2'></span>
      <%end%>
      <%if @hplace.avg_evalu.to_i  < 5%>
      <span class='glyphicon glyphicon-star-empty star3'></span>
      <%else%>
      <span class='glyphicon glyphicon-star star2'></span>
      <%end%><br>

<%if @hplace.place_photo1.to_s!=''%>
<br>

   <div id="myCarousel" class="carousel slide" data-ride="carousel">
    <!-- Indicators -->
    <ol class="carousel-indicators">
      <li data-target="#myCarousel" data-slide-to="0" class="active"></li>
      <%if @hplace.place_photo2.to_s!=''%>
      <li data-target="#myCarousel" data-slide-to="1"></li>
      <%end%>
      <%if @hplace.place_photo3.to_s!=''%>
      <li data-target="#myCarousel" data-slide-to="2"></li>
      <%end%>
      <%if @hplace.place_photo4.to_s!=''%>
      <li data-target="#myCarousel" data-slide-to="4"></li>
      <%end%>
      <%if @hplace.place_photo5.to_s!=''%>
      <li data-target="#myCarousel" data-slide-to="5"></li>
      <%end%>
    </ol>

    <!-- Wrapper for slides -->
    <div class="carousel-inner">
      <div class="item active">
            <img src="<%=asset_path 'hplace/'+@hplace.id.to_s+'/place_photo1/'+@hplace.place_photo1.file.filename.to_s%>" class="img-responsive"alt="<%=@hplace.place_photo1.file.filename%>"style="width:100%; height: 400px;">
      </div>
      <%if @hplace.place_photo2.to_s!=''%>
      <div class="item">
            <img src="<%=asset_path 'hplace/'+@hplace.id.to_s+'/place_photo2/'+@hplace.place_photo2.file.filename.to_s%>" class="img-responsive" alt="<%=@hplace.place_photo2.file.filename%>"style="width:100%; height: 400px;">
      </div>
      <%end%>
      <%if @hplace.place_photo3.to_s!=''%>
      <div class="item">
            <img src="<%=asset_path 'hplace/'+@hplace.id.to_s+'/place_photo3/'+@hplace.place_photo3.file.filename.to_s%>" class="img-responsive" alt="<%=@hplace.place_photo3.file.filename%>"style="width:100%; height: 400px;">
      </div>
      <%end%>
      <%if @hplace.place_photo4.to_s!=''%>
      <div class="item">
            <img src="<%=asset_path 'hplace/'+@hplace.id.to_s+'/place_photo4/'+@hplace.place_photo4.file.filename.to_s%>" class="img-responsive" alt="<%=@hplace.place_photo4.file.filename%>"style="width:100%; height: 400px;">
      </div>
      <%end%>
      <%if @hplace.place_photo5.to_s!=''%>
      <div class="item">
            <img src="<%=asset_path 'hplace/'+@hplace.id.to_s+'/place_photo5/'+@hplace.place_photo5.file.filename.to_s%>" class="img-responsive" alt="<%=@hplace.place_photo5.file.filename%>"style="width:100%; height: 400px;">
      </div>
      <%end%>
    </div>

    <!-- Left and right controls -->
    <a class="left carousel-control" href="#myCarousel" data-slide="prev">
      <span class="glyphicon glyphicon-chevron-left"></span>
      <span class="sr-only">Previous</span>
    </a>
    <a class="right carousel-control" href="#myCarousel" data-slide="next">
      <span class="glyphicon glyphicon-chevron-right"></span>
      <span class="sr-only">Next</span>
    </a>
  </div>
  <br>
<%end%>
<table class="table">
  <tbody>
    <tr><td class="text-center"><i class="glyphicon glyphicon-cutlery"></i><td><td><%=@hplace.intro%></td></tr>
    <tr><td class="text-center"><i class="glyphicon glyphicon-map-marker"></i><td><td><%=@hplace.place_address%></td></tr>
    <tr><td class="text-center"><i class="glyphicon glyphicon-earphone"></i><td><td><%=@hplace.place_call%></td></tr>
    <tr><td class="text-center"><span class="glyphicon glyphicon-home"></span><td><td><a href="http://<%=@hplace.site%>"><%=@hplace.site%></a></td></tr>
    <tr><td class="text-center"><i class="glyphicon glyphicon-time"></i><td><td><%=@hplace.optime%></td></tr>
    <tr><td class="text-center"><i class="glyphicon glyphicon-ok-circle"></i><td><td><%if @hplace.park="yes"%>주차 가능<%else%>주차 불가능<%end%></td></tr>
    <tr><td class="text-center"><i class="glyphicon glyphicon-thumbs-up"></i><td><td><%if @hplace.remarks&&@hplace.remarks!=""%><%=@hplace.remarks.gsub("\r\n", "<br>").gsub("\u0020","&nbsp;").html_safe%><%end%></td></tr>
  </tbody>
</table>

<div id="map" style="height:350px;" class="responsive"></div>
<script type="text/javascript" src="//dapi.kakao.com/v2/maps/sdk.js?appkey=106b3247f8c5679e18e7c2a2b29283f4&libraries=services"></script>
<script>
var mapContainer = document.getElementById('map'), // 지도를 표시할 div 
    mapOption = {
        center: new daum.maps.LatLng(37.5652706,126.9871768), // 지도의 중심좌표
        level: 3 // 지도의 확대 레벨
    };  

// 지도를 생성합니다    
var map = new daum.maps.Map(mapContainer, mapOption); 

// 주소-좌표 변환 객체를 생성합니다
var geocoder = new daum.maps.services.Geocoder();

// 주소로 좌표를 검색합니다
geocoder.addressSearch('<%=@hplace.place_address%>', function(result, status) {

    // 정상적으로 검색이 완료됐으면 
     if (status === daum.maps.services.Status.OK) {

        var coords = new daum.maps.LatLng(result[0].y, result[0].x);

        // 결과값으로 받은 위치를 마커로 표시합니다
        var marker = new daum.maps.Marker({
            map: map,
            position: coords
        });

        // 인포윈도우로 장소에 대한 설명을 표시합니다
        var infowindow = new daum.maps.InfoWindow({
            content: '<div style="width:150px;text-align:center;padding:6px 0;"><%=@hplace.place_name%></div>'
        });
        infowindow.open(map, marker);

        // 지도의 중심을 결과값으로 받은 위치로 이동시킵니다
        map.setCenter(coords);
    } 
});    
</script>
<br><br>
<%if user2_signed_in?%>
  <%if current_user2.level == 'admin'%>
    <a class="btn btn-info pull-right" role="button" onclick="location.href='/hplaces/<%=@hplace.category%>?order_by=<%=params[:order_by]%>&page=<%=@page%>'">뒤로가기</a>
    <a data-confirm="정말 글을 지우시겠습니까?" data-remote="true" data-method="delete"  href="/hplaces/<%=@hplace.id%>?page=<%=@page%>" class="btn btn-info pull-right" role="button">삭제하기</a>
    <a href="/hplaces/<%=@hplace.id%>/edit?page=<%=@page%>" class="btn btn-info pull-right" role="button">수정하기</a>
  <%else %>
      <a class="btn btn-info pull-right" role="button" onclick="window.history.go(-1)">뒤로가기</a>
  <%end%>
<%else%>
    <a class="btn btn-info pull-right" role="button" onclick="window.history.go(-1)">뒤로가기</a>
<%end%>

<br><br><hr><br><br>

<!--Comment script & style-->
<script>
var __slice = [].slice;

(function($, window) {
  var Starrr;

  Starrr = (function() {
    Starrr.prototype.defaults = {
      rating: void 0,
      numStars: 5,
      change: function(e, value) {}
    };

    function Starrr($el, options) {
      var i, _, _ref,
        _this = this;

      this.options = $.extend({}, this.defaults, options);
      this.$el = $el;
      _ref = this.defaults;
      for (i in _ref) {
        _ = _ref[i];
        if (this.$el.data(i) != null) {
          this.options[i] = this.$el.data(i);
        }
      }
      this.createStars();
      this.syncRating();
      /*this.$el.on('mouseover.starrr', 'span', function(e) {
        return _this.syncRating(_this.$el.find('span').index(e.currentTarget) + 1);
      });*/
      this.$el.on('mouseout.starrr', function() {
        return _this.syncRating();
      });
      this.$el.on('click.starrr', 'span', function(e) {
        return _this.setRating(_this.$el.find('span').index(e.currentTarget) + 1);
      });
      this.$el.on('starrr:change', this.options.change);
    }

    Starrr.prototype.createStars = function() {
      var _i, _ref, _results;

      _results = [];
      for (_i = 1, _ref = this.options.numStars; 1 <= _ref ? _i <= _ref : _i >= _ref; 1 <= _ref ? _i++ : _i--) {
        _results.push(this.$el.append("<span class='glyphicon .glyphicon-star-empty star1'></span>"));
      }
      return _results;
    };

    Starrr.prototype.setRating = function(rating) {
      if (this.options.rating === rating) {
        rating = void 0;
      }
      this.options.rating = rating;
      this.syncRating();
      return this.$el.trigger('starrr:change', rating);
    };

    Starrr.prototype.syncRating = function(rating) {
      var i, _i, _j, _ref;

      rating || (rating = this.options.rating);
      if (rating) {
        for (i = _i = 0, _ref = rating - 1; 0 <= _ref ? _i <= _ref : _i >= _ref; i = 0 <= _ref ? ++_i : --_i) {
          this.$el.find('span').eq(i).removeClass('glyphicon-star-empty').addClass('glyphicon-star');
        }
      }
      if (rating && rating < 5) {
        for (i = _j = rating; rating <= 4 ? _j <= 4 : _j >= 4; i = rating <= 4 ? ++_j : --_j) {
          this.$el.find('span').eq(i).removeClass('glyphicon-star').addClass('glyphicon-star-empty');
        }
      }
      if (!rating) {
        return this.$el.find('span').removeClass('glyphicon-star').addClass('glyphicon-star-empty');
      }
    };

    return Starrr;

  })();
  return $.fn.extend({
    starrr: function() {
      var args, option;

      option = arguments[0], args = 2 <= arguments.length ? __slice.call(arguments, 1) : [];
      return this.each(function() {
        var data;

        data = $(this).data('star-rating');
        if (!data) {
          $(this).data('star-rating', (data = new Starrr($(this), option)));
        }
        if (typeof option === 'string') {
          return data[option].apply(data, args);
        }
      });
    }
  });
})(window.jQuery, window);

$(function() {
  return $(".starrr").starrr();
});

$( document ).ready(function() {
      
  $('#stars').on('starrr:change', function(e, value){
    $('#evalu').val(value);    
  });
  
});
</script>
        <script>
          function textarea_content() {
            if (confirm('로그인이 필요한 기능입니다. 로그인을 하시겠습니까?')) {
              // 로그인페이지로 이동
              location.href="/user2s/sign_in";
            } else {
              // Do nothing!
            }
	          //alert("로그인을 하세요.");
          }
          function check() {
            if(!theForm.content.value.replace(/(^\s*)|(\s*$)/gi, "")){
            alert("후기를 입력해주세요.");
            theForm.content.focus();
            return;}
            else{
              document.theForm.submit();
            }    
          } 
        </script>
<style>
.star1{font-size: 2em; color: orange;}
</style>

<!--Comment -->
<!--수정-->
<%if user2_signed_in? && @click == 'updatecomment'%>
<form method="POST" action="/comments/update" accept-charset="UTF-8" name="theForm">
  <div class="form-group">
    <div class="row">
      <div class="col-sm-12">
        <label for="comment">리뷰 수정:</label>
      </div>
      <div class="col-sm-10">
        <textarea class="form-control" rows="3" id="content" name="content"><%=@wascomment.first.content%></textarea>
      </div>
      <div class="col-sm-2">
        <div id="stars" class="starrr"></div>
        <input id="id" type="hidden" name="id" value="<%=@wascomment.first.id%>">
        <input id="hplace_id" type="hidden" name="hplace_id" value="<%=@wascomment.first.hplace_id%>">
        <input id="evalu"  type="hidden" name="evalu"  value="0">
        //사진
        <input class="btn btn-info btn-lg" onclick="check();" value="등록">
      </div>
    </div>
  </div>
</form>
<!--등록-->
<%elsif user2_signed_in?%>
<form action="/hplaces/<%=@hplace.id%>/comments" data-remote="true" method="post" name="theForm">
  <div class="form-group">
    <div class="row">
      <div class="col-sm-12">
        <label for="comment">리뷰:</label>
      </div>
      <div class="col-sm-10">
        <textarea class="form-control" rows="3" id="content" name="content"></textarea>
      </div>
      <div class="col-sm-2">
        <div id="stars" class="starrr"></div>
        <input id="place_id" type="hidden" name="place_id" value="<%=@hplace.id%>">
        <input id="user2_id" type="hidden" name="user2_id"  value="<%=current_user2.id%>">
        <input id="user_id" type="hidden" name="user_id"  value="<%=current_user2.user_id%>">
        <input id="place_delete" type="hidden" name="place_delete"  value="no">
        <input id="evalu"  type="hidden" name="evalu"  value="0">
        //사진
        <%if @wascomment.length == 0%>
        <input class="btn btn-info btn-lg" onclick="check();" value="등록">
        <%else%>
        <input class="btn btn-info btn-lg disabled" value="리뷰작성완료">
        <%end%>
      </div>
    </div>
  </div>
</form>
<!--비로그인-->
<%else%>
  <div class="form-group">
    <div class="row">
      <div class="col-sm-12">
        <label for="comment">리뷰:</label>
      </div>
      <div class="col-sm-10">
        <textarea class="form-control" rows="3" id="content" name="content" onclick="textarea_content();"></textarea>
        <script>
          function textarea_content() {
            if (confirm('로그인이 필요한 기능입니다. 로그인을 하시겠습니까?')) {
              // 로그인페이지로 이동
              location.href="/user2s/sign_in"
            } else {
              // Do nothing!
            }
	          //alert("로그인을 하세요.");
          }
        </script>
      </div>
      <div class="col-sm-2">
        <div id="stars" class="starrr"></div>
        <input class="btn btn-info btn-lg" onclick="textarea_content();" value="등록">
      </div>
    </div> 
  </div>
<%end%>


<!--Comment list -->
<%if user2_signed_in?%>
<%@hplace.comments.each do |c|%>
<hr>
<form class="form-horizontal">
  <div class="form-group">
    <div class="col-sm-2">
    <h4><%=c.user_id%></h4>
    <%=c.created_at.strftime("%Y-%m-%d, %P %l:%M") %><br>
      <%if c.user2_id == current_user2.id%>
      <a href="/hplaces/<%=@hplace.id%>?click=updatecomment">수정</a> 
      <a data-confirm="리뷰를 정말 삭제하시겠습니까?" data-remote="true" data-method="delete"  href="/comments/<%=@wascomment.first.id%>?hplace_id=<%=@hplace.id%>">삭제</a> 
      <%end%>
      </div>
    <div class="col-sm-10">
      <%if c.evalu.to_i  < 1%>
      <span class='glyphicon glyphicon-star-empty star1'></span>
      <%else%>
      <span class='glyphicon glyphicon-star star1'></span>
      <%end%>
      <%if c.evalu.to_i  < 2%>
      <span class='glyphicon glyphicon-star-empty star1'></span>
      <%else%>
      <span class='glyphicon glyphicon-star star1'></span>
      <%end%>
      <%if c.evalu.to_i  < 3%>
      <span class='glyphicon glyphicon-star-empty star1'></span>
      <%else%>
      <span class='glyphicon glyphicon-star star1'></span>
      <%end%>
      <%if c.evalu.to_i  < 4%>
      <span class='glyphicon glyphicon-star-empty star1'></span>
      <%else%>
      <span class='glyphicon glyphicon-star star1'></span>
      <%end%>
      <%if c.evalu.to_i  < 5%>
      <span class='glyphicon glyphicon-star-empty star1'></span>
      <%else%>
      <span class='glyphicon glyphicon-star star1'></span>
      <%end%>
      <p class="form-control-static">
      <%=c.content.gsub("\r\n", "<br>").gsub("\u0020","&nbsp;").html_safe%>
      </p>
    </div>
  </div>
</form>
<br>
<%end%>
<hr>
<%else%>
<div class="container">
<img class="img-responsive center-block" src="<%=asset_path 'comment2.png'%>" alt="로그인 후 후기를 확인하세요." />
</div>
<%end%>


  </div>
